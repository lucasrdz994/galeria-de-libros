<!DOCTYPE html>
html(lang="en")
  include includes/head
  body
    main
      h1 #{pagina}
      if error
        span.alerta #{error}
      form(action='/reestablecer' method="POST")
            input(type="text" placeholder="Email" name="email")
            button.boton(type="submit") Reestablecer contraseña
      a(href="/iniciarSesion") Recorde mi contraseña